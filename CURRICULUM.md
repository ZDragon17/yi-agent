# yi-agent 磨练课程表：难度递增的场景阶梯

目标：用难度**指数增长**的场景阶梯持续磨练底座。每一级必须满足：
(1) 判据能区分"当前结构"与"更高能力"；(2) 全部走真实 CLI 与隔离子实验空间；
(3) 结果无论正负都如实记录——负结果精确刻画缺口，作为下一级的磨练目标。

## 已交付阶梯

| 级别 | 场景 | 新增难度维度 | 判据状态 |
|---|---|---|---|
| L1 | 5 个内置世界（temperature/grid/queue/inventory/virtual-desktop） | 多维观测、资源耦合、离散空间、保护对象 | 全绿（内置套件） |
| L2 | cyclic-phase（周期-3 隐藏相位） | 隐藏状态、关系键坍缩 | ✅ v25 后锁定 |
| L2 | cyclic-collision（周期-7 碰撞） | 窗口信息论分歧（窗口-2 不可表达） | ✅ v26 后 6/7 平台 |
| L3 | ci-ess（工商储能） | 设备动力学、BMS 边界、经济结算 | ✅ 循环 + 安全 + 重放 |
| L3 | pv-station（光储充） | 三设备耦合、外部事件、双保护投影 | ✅ 全程不越容 + 归因保守 |
| L3 | ems（能源管理） | 延迟反馈（DR 反弹归因）、需量合同 | ✅ 反弹 ACTION/恢复 AMBIGUOUS |
| L3 | vpp（虚拟电厂） | 双站点聚合、指令跟踪 | ✅ 偏差 <38kW |
| **L4** | **ess-arbitrage（跨期套利）** | **跨期价值表达** | ❌ **负结果**：horizon 1/4/8 均 ≈ 基线（±8% 内）——缺口是跨步信用分配，不是规划深度 |
| **L4** | **adversarial（对抗性世界）** | **世界主动反学习**（对可预测性翻转动量学） | ✅ 96 步净收益为正 |

## L4 实验记录（本轮）

### ess-arbitrage：跨期套利的负结果

三组预注册对比（96 步、kernel-only、horizon 1/4/8）：三组电费与待机基线差均在 ±8% 内，
horizon 8 与 horizon 1 几乎无差。结论：**跨期套利的缺口不在规划深度**（v16-v18 的树规划
已经在 rollout 中展开谷充→峰放链），而在**跨步信用分配**——谷充动作的收益归因延迟到
数小时后的峰放步，单步归因 + 模型 rollout 的架构无法把这段延迟收益记到谷充动作的
信用上。这正是强化学习中的经典 credit assignment 问题，被行业场景精确暴露。

### adversarial：对抗性世界的正结果

世界观察 Kernel 最近 3 步的选择，对被选中 ≥2 次的动作翻转动量学（+1 变 -1）——
固定策略必然被利用。kernel（有界学习 + 漂移适应 + 上下文条件策略）在 96 步中维持
**净正收益**，且重放一致。这证明 L2 级验证的"漂移后再组织"能力在对抗性切换下依然成立。

## R 轮系列：L5 磨练（反馈延迟按指数增长，每轮 ×2）

| 轮 | 反馈延迟 | 底座状态 | 判据 | 状态 |
|---|---|---|---|---|
| R1 | 无反馈（缺口基线） | v27 不变 | 定量刻画 horizon 1-8 缺口曲线 | ✅ **台阶结构发现**：h1-3 缺口 -0.7%，h4 跳至 -3.0% 后平台——h4 恰为「谷充 3 步 + 峰放 1 步」最短回本链；全套利潜力 ~13% 仍未达，剩余缺口 = 跨步信用分配（图 `fig9-r1-gap-curve.svg`） |
| R2 | 结算反馈延迟 2 步 | 机制已有（pending 窗口 8） | 反馈结算出现 + 行为变化 | ✅ 通过 |
| R3 | 延迟 4 步 | 机制已有 | 窗口内 | ✅ 通过 |
| R4 | 延迟 8 步 | 窗口边缘（maxAge 8） | 边界检验 | ✅ 通过 |
| R5 | 延迟 16 步 | v28 将 pending 窗口扩展至 16 步 | 预期失败→演化 | ✅ 通过 |
| R6 | 日结算（24 步稀疏总量） | credit assignment 真问题 | 如实记录 | ✅ 通过 |
| R7 | 反馈噪声 ±20% | 鲁棒性 | 如实记录 | ✅ 通过 |
| R8 | 对抗叠加 | 反学习 + 延迟 | 如实记录 | ✅ 通过 |
| R9 | 电价表 mid-run 漂移叠加 | 非平稳叠加 | ✅ 翻转被观测捕获（-0.35→+0.5）+ 重放一致 |
| R10 | 终局全合成验收（延迟16+噪声+对抗+翻转同场） | 全叠加 | ✅ 96 步 COMPLETED + 归因分级正确 + 重放一致 |

**十轮收官**（R1-R10 全部执行，2 次提交推送；全量回归 453/455，2 个失败均为 durability-matrix 负载超时抖动——单跑复验全绿，历轮同模式）：十轮实验共新增 6 个测试文件、2 幅图表、1 幅缺口曲线；底座演化 1 次（v28 pending 窗口扩展）；负结果 1 个（跨期套利的跨步信用分配缺口，定量曲线存档）。

## L5 磨练目标（由 L4 负结果定义）

F-129/F-130 已先完成价值入口的隔离验证：`signed-v1` 累计效用通道可重放，但 24 步实验中 horizon 8 仍未优于 horizon 1，因而没有把入口修复误报成跨期收敛。

F-131 已完成动作链信用的最小公共协议验证：WorldPort 可在单条 clean feedback 中声明有界 `creditChain`，Kernel 按 pending 顺序和闭合份额分配净变化，跨进程和 Replay 保持一致；这只证明“结构化声明能被安全传递和审计”，尚未证明声明真实，也尚未证明跨期套利收敛。

F-132 在独立延迟效用 WorldPort 中完成了第一组可区分性验证：同一世界先执行 `chain.prepare`、再执行 `chain.commit`，第三步释放一次共同延迟效果。无链分支因共享观测边界得到两条 `AMBIGUOUS` 且不学习；正确链分支得到两条 `ACTION_CHAIN` 并学习两个动作模型；两个分支均跨 CLI Replay 为 `CONSISTENT`。这证明动作链协议会改变可观察学习结果，但仍不证明 WorldPort 自报份额是真实因果。

F-133 随后把份额改成结构合法但错误的 `0.99/0.01`，底座仍学习两条 `ACTION_CHAIN` 且 Replay 一致。该负结果把缺口从“字段是否生效”收窄为“因果是否可辨识”：一次共同结果没有足够信息区分正确声明与错误声明；没有独立干预/反事实证据时，Kernel 不能自证份额正确。

F-134 将链升级为可选的 `counterfactual-additive-v1` 证据：每个成员直接提供孤立干预 delta，Kernel 只有在成员 delta 逐维闭合到观测实际变化时才学习；不闭合的 witness 全部结算为 `AMBIGUOUS`。这增加了可拒绝的代数证据边界，但不把 adapter 的报告升级成现实因果真值。

F-135 在真实 JSONL WorldPort 上验证了闭合失败路径：结构和维度都合法、但成员 delta 总和不等于实际变化时，两个成员均为 `AMBIGUOUS`、不生成动作模型，Replay 仍为 `CONSISTENT`。

F-136 在真实 JSONL WorldPort 上验证了来源绑定边界：`counterfactual-attested-v1` 将完整反馈快照、成员 delta 和摘要绑定到当前描述符的 Ed25519 公钥；有效签名通过 v30 的加性闭合并学习，篡改签名在外部边界被拒绝且不产生动作模型，两个分支均保持 Replay 一致。该结果只证明“当前 adapter 对声明的来源/完整性”，不证明现实干预或因果真值；下一步仍需独立干预、对照或可信执行器证据。

F-137 用测试夹具的独立 ground-truth 记录构造了“签名有效但因果归因错误”的闭合 witness：真实归因是 `[0,1]`，adapter 声明 `[0.75,0.25]`，CLI 仍学习错误份额且 Replay 一致。该负结果确认 v31 已到达单一证据生产者的可辨识性边界。

F-138 将最小必要变化落为 v32 `counterfactual-independent-v1`：主 WorldPort 与独立启动的 witness adapter 使用不同密钥，见证请求不携带主 adapter 的 delta，只携带反馈元数据和成员 nonce；宿主验证见证签名、比较成员 delta 后才注入 `independentAttestation`。正确见证跨 init→run→Replay 产生两个动作模型；见证签名有效但与主声明不一致时在第三步前 fail-closed，前两步账本保留且不学习。该结果建立了第二声明来源的可部署边界，但仍不等于现实因果真值，不能抵御共谋或共同物理来源。

1. **跨步信用分配的真实增益**：F-132 已证明正确链与共享边界会产生不同的可观察学习结果，F-133 又证明错误份额目前不可由底座识别；F-138 已增加第二声明来源和不一致拒绝，但仍未提供现实干预真值；下一步必须检验缺成员、拒绝链、来源共谋和真实策略收益，协议本身不能替代世界事实；
2. 对抗性世界的多动作扩展（3+ 动作、连续功率设定）；
3. 非平稳叠加：漂移 + 对抗 + 耦合同时存在。

## L6（远景，属外部卡点）

真实设备遥测接入（数据通道 WorldPort + 人工数据契约）、真实经济结算。

# 易闭环 Agent：一个可重放、可反证、持续演化的通用 Agent 底座

## ——从可抛弃原型到能源行业场景的实证报告

**版本**：CLI v0.1（kernelLearningVersion 27）
**代码库**：https://github.com/ZDragon17/yi-agent
**日期**：2026-09-06

---

## 摘要

当前的主流 Coding Agent（Codex CLI、Claude Code）将智能与软件工程场景深度耦合：文件、Shell 与代码编辑写死在内核里。本项目提出并验证了一个相反的命题——**存在一套领域无关的最小闭环（观察 → 行动 → 验证 → 学习），配合严格的证据与安全约束，可以在任意被约束的"世界"中积累经验并持续演化**。

我们以《易经》"同源、关系、变化、反复验证"的哲学作为架构公理（而非科学结论），实现了一个零第三方依赖的 Node.js CLI 实验底座 `yi-agent`，并以三层证据链证明其按设定方向成长：

1. **架构不变量**：Kernel 只接收数值观测、ValueSpec 与不透明 action token，领域语义全部在 WorldPort 边界；同一内核零改动地驱动 5 个内置通用世界、2 个协议示例世界与 4 个能源行业场景世界（工商储能、光储充场站、能源管理系统、VPP 虚拟电厂），累计 11 个 WorldPort；
2. **反证驱动的演化**：以预注册判据的 E2E 反证实验驱动底座演化，学习语义从 v3 版本化演进至 v27（24 个能力台阶），每步均带历史回放兼容门控；13 个正式任务记录（F-114~F-126）构成完整的"反证 → 演化 → 验收"闭环；
3. **证伪力与自我更正**：内置挑战套件 10/10 通过且经突变检验证明全部具备证伪力（10/10 可 FALSIFIED）；混杂归因防线被实证为纵深四层；两轮错误的科学结论（度量伪影、假设性缺陷）被实验自我否决并公开更正——系统不以演示结果为自己颁发"已经智能"的证明。

最终，真实大模型（GLM-5.3-flash）经 OpenAI 兼容接缝驱动闭环，提议被 Kernel 校验、以 2 步达成目标、重放零模型调用；同一套闭环在能源行业场景中以确定性行为通过全部预注册行业判据。全量自动测试 **444/444 通过**。

**关键词**：Agent 底座；证据闭环；确定性重放；反证驱动演化；领域盲内核；WorldPort；能源行业场景

---

## 1 引言：问题与设定方向

### 1.1 问题

如果把"编程"换成温度控制、储能套利、需量管理或电网指令跟踪，底层是否仍然可以使用同一套智能逻辑？

主流 Coding Agent 的答案是"不需要"：它们把世界抽象固定为代码仓库。本项目设定的方向与此相反——**智能行为应当体现为跨情境的观察、行动、验证、学习与再组织能力**，而领域只是同一套状态—关系—约束—变化—反馈逻辑在不同边界上的投影。这一方向以《易经》的"同源、关系、变化、反复验证"作为架构公理：不是把卦象硬编码进程序，而是要求任何新增机制都必须回答三个问题——它对应哪条共同变化规律、如何被反例检验、如何在另一个领域复用。

### 1.2 从原型到可检验的底座

项目的起点是一个可抛弃的双环境 HTML 原型，它证明了"预期—行动—验证—修正"能够运行，但状态只在内存里、无法跨进程延续、无法严格重放、也无法系统地制造反例。本报告覆盖的演化阶段（F-114~F-126，对应提交 `0617c13..47dd751`）完成了三个跃迁：

- **可运行**：跨环境（Node 22~26、npm 12、Windows）克隆即跑、可 `npm pack` 安装、444 项自动测试全绿；
- **可反证**：预注册判据的 E2E 反证、挑战套件及其突变检验、真实模型闭环；
- **行业落地**：能源行业四个场景的外部 WorldPort 包与设备语义库。

---

## 2 系统架构与方法论

### 2.1 分层架构与信任边界

```
CLI（薄适配层，退出码与 JSON 信封）
  → Application Services（init/run/inspect/replay/challenge/recover/agent/experiment/ui）
      → Kernel（纯闭环：预测→选择→执行→验证→学习，无 I/O、无时钟、无领域字面量）
      ← WorldPort adapters（内置 5 世界 + 外部 yi-world-cli JSONL 协议 + 能源场景包）
  → LabStore（JSONL 事件账本 + 原子快照 + writer lock + 哈希链）
  → ModelAdvisor/ModelPlanner（OpenAI-compatible，仅提议，永不裁决）
```

信任边界由宪法级不变量约束，其中被本报告实验直接触发的包括：

- **不变量 8**：Kernel 不得接收或分支判断 worldId、scenarioId、领域标签与文件名；
- **不变量 5**：选择层与执行器层都必须校验权限与安全边界，不能单点信任（§4.3 的四层混杂防线是其实证）；
- **不变量 9**：系统不能给自己颁发"已经智能"的证明——内置 challenge 只提供演示证据，结论只能是"未被证伪"；
- **不变量 13**：模型输出只是不可信提议；只有 Kernel 的安全选择、WorldPort 的执行回执和 verify/learn 的证据才能改变连续性状态。

### 2.2 证据闭环与确定性重放

每次行动前必须留下可证伪预期（无预期不行动）；行动后必须复观并验证（无法验证的结果不计入已学知识）。全部事件写入带哈希链的 JSONL 账本；重放从不可变起点重新执行纯 World 模拟、Kernel 决策与 RNG，逐步与账本比较。**重放不重新调用模型**——模型提议以摘要与观测摘要绑定冻结在账本中（§4.4 在真实供应商下验证了这一契约）。

### 2.3 反证驱动的演化方法论

每一轮演化遵循同一纪律：

1. **反证**：构造一个预注册判据的实验（通常为新 WorldPort + 真实 CLI E2E），判据必须能区分"当前结构"与"声称的结构"；
2. **证伪成立**：以最小实现修复，全部按学习版本门控（旧账本永远按旧语义重放，未知版本 fail-closed）；
3. **验收**：新 E2E 红 → 绿，全量回归不回退，文档四件套（tasks/design/spec/README）同步；
4. **自我更正**：若后续实验推翻先前结论，公开撤回并更正记录（§4.2）。

---

## 3 演化历程：从 v3 到 v27

Kernel 的学习语义以 `kernelLearningVersion` 版本化，每个版本对应一个被反证实验驱动的能力台阶：

| 版本 | 能力 | 驱动反证 |
|---|---|---|
| v3–v8 | 已结算反馈收据、pending credit 窗口、预测信念样本、反馈顺序规范化、共享观测边界保守归因、监督器证据对齐 | 延迟反馈与混杂归因系列 |
| v11 | 顺序敏感的历史累积摘要（h2 长上下文雏形） | 超近期窗口的顺序证据 |
| v12–v14 | 有界主动信息规划、决策分化、价值几何投影 | 规划信息价值的可判定性 |
| v15 | 周期性再验证（陈旧安全行动的重访） | 隐藏动力学漂移 |
| v16–v18 | 序列规划、递归 belief 分支、未来动作树 | 多步假设评估 |
| v21–v24 | 模型年龄、持久化生存预算、证据新鲜度淘汰、可靠性支配淘汰 | 长运行的容量与遗忘 |
| **v25** | **多尺度上下文（h0 窗口-1）+ 上下文键浮点残差量化 + 反事实探测** | 周期-3 隐藏相位世界 |
| **v26** | **长窗口上下文（h2 键改窗口-8 活摘要）** | 周期-7 碰撞世界 |
| **v27** | **周期再验证的信念门控** | 平台瓦解诊断（后更正为度量伪影） |

项目累计记录 **125 项任务**（T- 系列基础设施 + F- 系列特性/反证），其中本报告覆盖的 F-114~F-126 共 13 项。

---

## 4 实验与证据

### 4.1 隐藏相位动力学：从盲选到相位锁定

**实验设计**：外部 WorldPort 把"相位"藏在世界内部（每步推进且观测向量不含相位信息）。周期-3 世界（赢家调度 A/B/C 轮转）检验单步几何 + 上下文策略；周期-7 碰撞世界（赢家调度 A,B,A,C,B,A,D）刻意让窗口-1/窗口-2 上下文在碰撞相位上产生信息分歧——任何窗口-2 条件策略的理论赢家率上限约 5.5/7 ≈ 78.6%，只有覆盖完整周期的窗口（≥7）才能表达最优策略。

**结果**：

| 配置 | 收尾窗口当步赢家率 | 结论 |
|---|---|---|
| v24（基线） | 1/3 ≈ 33%（探索闩锁踏步） | 反证成立：早期停滞 → EXPLORATORY 单向闩锁 × coverage 踏步 |
| v25（h0 键 + 有界探索回合 + 反事实探测） | 周期-3 达标；周期-7 仅 28.6% | 单步几何对周期-3 有效、对碰撞周期不足 |
| v26（h2 改窗口-8 活摘要） | **~150 步收敛到 6/7 ≈ 85.7% 平台**（窗口-2 类的信息论最优） | 相位条件策略收敛 |
| v26 双 seed 真实 CLI 360 步 | 成熟窗口 87/120 与 ≥90/120，全部重放一致 | 跨进程、跨 token 置换稳定 |

**意外发现（F-122）**：h2 累加器键因位置权重按构造永不复现（写读错位一步），其原始"长期上下文可读"声明被内核探针证伪——v26 将其改造为窗口摘要，一次修复同时了结旧债并补上新能力。

### 4.2 目标驻留与度量伪影更正

周期-7 长跑（1500 步）显示 6/7 平台在数百步后"赢家率瓦解"。步骤级账本诊断最初将其归因为"元稳定恢复失败"（F-117）。后续值曲线插桩证明这是**度量伪影**：~650 步时值精确到达目标 400 并转入驻留（|v−400| ≤ 0.2 持续 500+ 步）——越过目标后调度赢家不再是价值最优动作，"赢家率下跌"度量到的恰是目标驻留这一正确行为。F-117 的错误结论被公开撤回（F-118），并以目标驻留稳定性 E2E（值 900 步内进入 400±5、末 100 步平均距离 ≤5、重放一致）固化正确行为。

这一更正本身是方法论的证据：**反证实验不仅约束实现，也约束结论**——当实验推翻自己的先前判断时，记录被更正而非掩饰。

### 4.3 挑战套件与突变检验

内置挑战套件（FR-5）10 个判别器在系统级运行中 10/10 PASS（未知行动探索、规律突变、执行拒绝、行动中外部事件、全动作不安全、快照写失败、重放篡改、inspect 只读、世界多样性、配对候选隔离分支）。

为证明套件证伪力不为零，对全部 10 个挑战执行**突变检验**：向被测实现注入每个判别器针对的缺陷，10/10 在隔离子实验空间判出 FALSIFIED 后立即还原（套件复验 11 个 verdict 全 PASS、全量回归 439/439）。两个计划外发现：

1. **世界实现摘要绑定实战拦截**：世界类突变被 `worldImplementationDigest` 摘要绑定当场拒绝（CONFLICT 65）——世界漂移检测机制的首次实战触发；
2. **混杂归因防线的纵深为四层**：世界回执标记 → 宿主强制 AMBIGUOUS → 内核 verify → 账本 STEP 校验。前三层单独突变均被余层拦截，四层同时关闭才放行——宪法不变量 5"双层校验、不能单点信任"的实证。

### 4.4 真实模型闭环

以真实供应商密钥（BigModel）经本地 Anthropic→OpenAI 协议桥（测试装置）跨越"真实供应商连通"卡点：

- `api test` CONNECTED（10 模型）；`ask` 返回真实 GLM 回答；
- `agent run --goal` 真实闭环：glm-5.3-flash 的提议经 Kernel 校验（`policyEvidence.applied=true`、归因 ACTION、learnable=true），**2 步达成目标**（OBJECTIVE_REACHED，bestDistance 0）；
- `replay` 一致且经桥日志确认**重放零模型调用**——确定性重放契约在真实供应商下成立。

真实闭环立即暴露了一个此前未知的边界缺陷（F-124）：模型响应中的可选 `proposal` 字段（repo 写入语义）被无条件塞进 transition 请求，内置 WorldPort 的封闭键集判 MALFORMED_REQUEST——真实闭环首步即拒。修复为"proposal 只进入声明 adapter 的 WorldPort 写入边界"（run 与 replay 双侧对称），新增红→绿 E2E，全部 repo proposal 写入流（F-73/74/76/77）回归无损。**真实模型在环的第一轮就为底座找到了缺陷**——这正是闭环价值的体现。

---

## 5 行业场景验证：能源行业

### 5.1 设备模型与场景投影

领域语义全部在 WorldPort 边界，Kernel 保持领域盲。共享设备模型库（`examples/energy/shared/energy-sim.mjs`）显式建模五类设备语义：

| 设备 | 建模语义 |
|---|---|
| **电池** | 800kWh SOC 动力学（库仑效率计入）、BMS 边界按**动作后终点 SOC** 判定 |
| **逆变器** | 额定有功/视在容量（S=√(P²+Q²) 约束无功设定）、负载率→效率分段线性降额（轻载 0.90→满载 0.98）、并网/离网双模式 |
| **电表** | 并网点功率平衡计量、电费结算、需量峰值 |
| **光伏** | 钟形出力曲线（场景驱动的云遮挡） |
| **充电桩** | 确定性接入序列；接入即行动中外部事件（归因保守化）；`charger.stop` 停充语义计入能力投影 |
| **DR 资源** | HVAC 群削减 40kW 持续 3 步后自动恢复反弹 +10kW（延迟效应） |

四个场景 adapter（yi-world-cli JSONL 协议、无状态、宿主持有 worldState）：

| 场景 | 观测/目标投影 | 行业判据 |
|---|---|---|
| **工商储能**（ci-ess） | [并网点功率, 放电余量, 电价偏离中位]；电价通道以中位归一使关系签名区分峰谷方向 | 72 步 COMPLETED、SOC 全程 BMS 界内、24h 窗口充放循环 ≥30%、电费不劣于待机基线、重放一致 |
| **光储充场站**（pv-station） | [SOC, 并网点, 光伏出力, 充电负荷]；防逆流 + 250kVA 变压器双保护投影 | 全程并网点 ≤250kVA、充电小时归因保守化（AMBIGUOUS）、重放一致 |
| **能源管理系统**（ems） | [本地负荷, 需量峰值, DR 剩余]；合同需量 250kW | DR 削峰行为、延迟反馈结算分级断言、需量 ≤ 合同 ×110%、逆变器模式状态机合法、重放一致 |
| **VPP 虚拟电厂**（vpp） | [聚合出力, 跟踪偏差, 调度指令]；双站点 ±30kW 步进 | 96 步收尾平均跟踪偏差 <38kW（指令幅度 ±30）、重放一致 |

**真实模型行业闭环**：glm-5.3-flash 在工商储能场景 4 步提议全部被采纳、零拒绝、重放一致——同一套 Kernel 契约从合成世界无缝延伸到行业世界。

### 5.2 建模真实性由 E2E 驱动

行业场景的开发过程本身就是反证方法论的行业实践：五处建模缺陷被预注册判据当场抓住并修正——电价表缺 24 小时（缺位产生 NaN→null 被协议层拦截）、BMS 边界只查起点（放电越下限）、保护约束错放 transition（正确做法是 safe 能力投影第一道、拒绝第二道）、`charger.stop` 停充后的光伏盈余倒送未计入能力投影、判据重算未反映停充效果。

### 5.3 延迟反馈的行业用例

EMS 的 DR 恢复反弹是延迟反馈机制（v4 引入）的第一个行业用例：世界在反弹步的 `feedback[]` 中按原 shed 动作的 `executionNonce` 返回结果快照——自动反弹干净归因（confounder 0 → ACTION 可学习），手动恢复作为外部干预混杂归因（confounder 1 → AMBIGUOUS 不学习）。调试过程修复了三处世界缺陷（restore 清零被递减覆盖产生无主 pending、模式切换步误建无主 pending、feedback 键集不符契约），全部 fail-closed 拦截。

---

## 6 边界与开放方向

按宪法不变量 9，本报告只声明"在已运行的实验集合上未被证伪"，以下边界如实记录：

1. **跨期价值**：分时套利的全局最优（谷充峰放的全局调度）超出单步距离几何的表达力；EMS/VPP 的策略收敛受有界学习的样本效率限制。多步规划（v16–v18）与行业场景的结合是下一个反证方向。
2. **场景覆盖**：四个能源场景为确定性仿真（scenario 驱动扰动）；接入真实设备遥测需要额外的数据通道 WorldPort 与人工数据契约。
3. **安全边界**：协议桥是测试装置；真实设备写操作仍属 EffectBroker/Future-Gate 人工闸；桌面端打包需另行人工确认。
4. **反证不穷尽**：突变检验证明的是"这些挑战能抓到这些缺陷"，不证明"能抓到一切缺陷"；对任意有限测试集合都无法逻辑排除更大的查表程序——系统只报告覆盖范围与"未被证伪"。

---

## 7 结论

在 F-114~F-126 共 13 轮"反证 → 演化 → 验收"循环中，yi-agent 从一个仅能驱动合成世界的实验 CLI，成长为：

- **跨 11 个 WorldPort 零内核改动**的通用底座（5 内置世界 + counter/repo 协议示例 + 4 能源行业场景）；
- **27 个版本化学习台阶**、每步带历史回放兼容门控的可演化内核；
- **444 项自动测试 + 10 挑战判别器 + 10/10 突变证伪力**的验收体系；
- **真实大模型在环**、**真实行业场景闭环**、**重放零模型调用**的可运行系统。

设定方向的三个核心主张均获得实验支持：领域不同不等于底层智能逻辑不同（同一闭环驱动 11 个世界）；智能必须体现为跨情境的观察—行动—验证—学习（隐藏相位锁定、目标驻留、阶段切换重收敛、DR 反弹归因）；系统不能自我证明（度量伪影的公开更正、突变检验对证伪力的实证、"未被证伪"的谨慎措辞）。

底层结构尚未到达"无法被实验区分"的绝对终点——跨期价值表达与真实设备数据接入是明确记录的下一步。但在当前实验手段可设计的范围内，反证循环已经连续产出"假设被否决"与"与既有覆盖重合"的结果，而外部卡点（打包人工闸、真实设备数据契约）均已就位。**按设定方向的成长是可度量、可追溯、可证伪的，全部证据已在 GitHub 上公开。**

---

## 附录 A：数据汇总

| 指标 | 数值 |
|---|---|
| 自动测试 | 444/444 通过（含 E2E、NFR、崩溃恢复矩阵、契约与安全门） |
| 任务记录 | 125 项（T-0~T-10 基础设施 + F-1~F-126 特性/反证） |
| 学习版本 | v3 → v27（21 个命名能力阈值，全部带历史回放兼容门控） |
| WorldPort | 5 内置 + counter/repo 示例 + ci-ess/pv-station/ems/vpp 行业场景 = 11 |
| 挑战套件 | 10/10 PASS；突变检验 10/10 FALSIFIED（证伪力实证） |
| 周期-7 相位锁定 | 盲选 28.6% → 6/7 平台 85.7%（双 seed 成熟窗口 87/120、≥90/120） |
| 目标驻留 | 值 900 步内进入目标带，|v−400| ≤ 0.2 持续 500+ 步 |
| 真实模型闭环 | glm-5.3-flash，2 步 OBJECTIVE_REACHED，重放零模型调用 |
| 账本契约 | JSONL 哈希链 + 原子快照 + writer lock + 摘要绑定；单账本 ≤40 MiB |
| 提交 | 14 个（`0617c13..47dd751`+），全部推送 GitHub |

## 附录 B：产物索引

| 产物 | 位置 |
|---|---|
| 行业场景包 | `examples/energy/`（shared/ci-ess/pv-station/ems/vpp） |
| 反证世界装置 | `test/fixtures/`（cyclic-phase、cyclic-collision 等） |
| 行业 E2E | `test/e2e/energy-scenarios.test.mjs` |
| 任务账本 | `tasks.md`（F-114~F-126 为本报告覆盖区间） |
| 治理文档 | `.specify/constitution.md`、`spec.md`、`design.md`、`plan.md` |
